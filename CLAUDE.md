## フォルダ構成

- `doc/`: 開発用の仕様書置き場
- `.claude/rules/`: 開発プロセス・ルールに関するファイル置き場
- `研修内容検討/`: 研修(座学・デモ)内容の検討用ファイル置き場

## チャットでの情報提供ルール

- ハルシネーションは絶対にしない。不明点は「不明」と回答する
- なるべく公式ドキュメントを参照し、情報提供時はソース(参照元)も添える

## ドキュメント作成・更新のルール

### 書かないこと（メンテナンス負荷を抑える）

- **ソースコードを読めばわかること**: 関数の実装詳細、具体的なコード例（コードとドキュメントの乖離が発生する）
- **他のドキュメントと重複する内容**: 同じ情報を複数箇所に書かない。必要ならリンクで参照する
- **自動取得可能な情報**: ディレクトリツリー（`tree`/`ls` で取得可能）、依存パッケージ一覧（`package.json` で確認可能）

### 図表

- 設計変更時は対応する図表も同時に更新してコードとの乖離を防ぐ

### mistakes.md への追記ルール

@.claude/rules/mistakes.md

セッション中にユーザーから訂正を受け、
かつ以下3条件をすべて満たすときのみ `.claude/rules/mistakes.md` に追記:

1. ユーザーからの明示的な訂正である(自分の気づきではない)
2. 繰り返し起こり得るパターンである(一度きりの偶発ではない)
3. 具体的な「する/しない」で書ける

追記前に既存エントリを確認し、類似のエントリがあれば新規追加ではなく更新する。
エントリが10件を超えたら、統合できるものを統合し、再発しなくなった古いものを削除する。

形式:

```
YYYY-MM-DD: [一言で何を間違えたか]
**NG Action**: 実際にやってしまった間違い
**Correct Action**: 次回からの正しい対応
**Trigger**: このルールが適用される状況
```
